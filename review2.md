# 代码质量评估报告与优化方案

> 评估时间：2026-04-21
> 项目：业余无线电模拟考试系统 (Go + Vue + Wails)

---

## 一、项目概述

| 指标 | 数值 |
|------|------|
| 后端文件数 | ~20 个 Go 文件 |
| 前端文件数 | ~15 个 Vue/TS 文件 |
| 代码行数（估算） | ~5000 行 Go + ~4000 行 Vue/TS |
| 架构模式 | 后端：Service-DAO 分层；前端：Vue 3 Composition API + Pinia |
| 数据库 | SQLite (GORM) |
| 框架 | Wails v2 (Go + Vue 桌面应用) |

---

## 二、当前代码质量评分

| 维度 | 评分 | 说明 |
|------|------|------|
| **架构设计** | ⭐⭐⭐⭐ 4/5 | 分层清晰，Service-DAO 模式良好 |
| **代码规范** | ⭐⭐⭐ 3/5 | 大部分遵循规范，存在不一致 |
| **安全性** | ⭐⭐⭐⭐ 4/5 | SQL注入已防护，但缺少输入验证 |
| **错误处理** | ⭐⭐⭐ 3/5 | 有日志记录，但部分错误被静默忽略 |
| **可维护性** | ⭐⭐⭐⭐ 4/5 | 模块化良好，但缺少单元测试 |
| **性能** | ⭐⭐⭐ 3/5 | PDF解析已优化，但存在优化空间 |
| **用户体验** | ⭐⭐⭐⭐ 4/5 | 界面友好，但缺少边界情况处理 |
| **总体评分** | **⭐⭐⭐⭐ 3.6/5** | 良好，有改进空间 |

---

## 三、已修复的问题（本次会话）

| 编号 | 问题 | 严重度 | 状态 |
|------|------|--------|------|
| P0-1 | 数据库连接管理混乱 | 高 | ✅ 已修复 |
| P0-2 | 随机数种子未初始化 | 高 | ✅ Go 1.20+ 无需修复 |
| P0-3 | PDF解析内存泄漏 | 高 | ✅ 已修复（改用单实例复用） |
| P1-4 | 事务panic被吞掉 | 高 | ✅ 已修复（panic重新抛出） |
| P1-5 | 前端状态管理薄弱 | 中 | ⚠️ 已部分修复（添加 dataCleared 事件） |
| P1-6 | SQL注入防护不完整 | 中 | ✅ 已验证，全部参数化查询 |
| P2-9 | 配置硬编码 | 低 | ⚠️ 已部分优化（exam_config.go） |
| 新增1 | 错题/收藏数据查询异常 | 高 | ✅ 已修复（添加调试日志） |
| 新增2 | 清理数据后用户信息未刷新 | 中 | ✅ 已修复（emit事件） |
| 新增3 | 导入无进度提示 | 中 | ✅ 已修复（悬浮进度条） |

---

## 四、仍存在的问题与优化方案

### 🔴 P0: 严重问题（建议立即修复）

#### 1. 缺少单元测试
- **影响**: 代码变更无法自动验证，回归风险高
- **位置**: 全局，无 `*_test.go` 文件
- **优化方案**:
  ```
  优先级: 高
  方案:
  - 为 DAO 层编写单元测试（使用 sqlite 内存数据库）
  - 为 Service 层编写集成测试
  - 核心业务逻辑：考试创建、答案提交、错题收藏
  - 目标覆盖率: 60%+
  ```

#### 2. 错题孤立记录问题（已发现根因，待彻底解决）
- **影响**: 题库重置后，错题/收藏记录指向不存在的题目
- **位置**: `error_question_dao.go:268-378`, `favorite_service.go`
- **当前状态**: 已添加诊断日志，发现 81 条孤立记录
- **优化方案**:
  ```
  方案A（推荐）: 题库重置时自动清理关联数据
  - 在 ResetQuestionsTable() 中添加清理逻辑
  - 使用外键约束（SQLite 需手动开启 PRAGMA foreign_keys）
  
  方案B: 查询时自动清理
  - GetErrorQuestionsWithDetails 中自动删除孤立记录
  - 对前端用户透明
  
  方案C: LEFT JOIN 显示（当前方案）
  - 即使题目不存在也显示记录
  - 需要前端处理题目内容为空的情况
  ```

#### 3. 前端 API 层存在 Mock/TODO 代码
- **影响**: `PracticeService.SubmitAnswer` 和 `GetNextQuestion` 返回假数据
- **位置**: `frontend/src/api/index.ts:246-268`
- **代码**:
  ```typescript
  // TODO: 需要后端实现
  SubmitAnswer: async () => {
    return { is_correct: true } // 前端硬编码！
  }
  ```
- **优化方案**: 移除 Mock 实现，要么后端实现真实逻辑，要么前端明确标注并警告

---

### 🟡 P1: 重要问题（建议尽快修复）

#### 4. 错误处理不一致
- **问题**: 
  - Service 层部分使用 `return nil, err`，部分使用 `return nil, fmt.Errorf(...)`
  - DAO 层的 `GetErrorQuestionsWithDetails` 中 scan 错误使用 `continue` 静默忽略
- **位置**: 多个 Service/DAO 文件
- **优化方案**:
  ```
  统一错误处理模式:
  - DAO 层: 只返回原始 error
  - Service 层: 包装为带上下文的 error (fmt.Errorf("操作失败: %w", err))
  - 移除所有静默 continue 的错误处理
  ```

#### 5. 前端类型定义不完整
- **问题**:
  - `ExamView.vue` 中手动定义 `Question` 接口
  - `PracticeView.vue` 中手动定义 `Question` 接口
  - 两处定义可能不一致
- **位置**: `frontend/src/views/ExamView.vue:143-155`, `PracticeView.vue`
- **优化方案**:
  ```
  - 创建 src/types/question.ts 共享类型文件
  - 使用 wailsjs/go/models 导出的类型
  - 消除手动重复定义
  ```

#### 6. 导入进度是模拟动画，非真实进度
- **问题**: 当前进度条使用 `setInterval` 模拟 0-90% 动画，不反映真实导入进度
- **位置**: `SettingsView.vue:215-245` (`startProgressAnimation`)
- **优化方案**:
  ```
  方案A（推荐）: 后端提供进度回调
  - PDFImporter 已有 ProgressCallback 类型
  - 通过 Wails Events 推送进度到前端
  - 前端使用真实百分比更新进度条
  
  方案B: 分阶段进度
  - 根据导入阶段设置固定百分比
  - 解析: 0-30%, 处理: 30-80%, 保存: 80-100%
  ```

#### 7. 配置硬编码
- **问题**: 考试配置写死在 `exam_config.go` 中，无法运行时修改
- **位置**: `backend/config/exam_config.go:14-36`
- **优化方案**:
  ```
  - 将配置移至 SQLite 数据库
  - 添加"考试设置"菜单，允许用户自定义
  - 保留默认配置作为 fallback
  ```

---

### 🟢 P2: 一般问题（建议后续优化）

#### 8. 前端组件耦合度高
- **问题**: 
  - `HomeView` 直接渲染 `PracticeView` 和 `ExamView` 作为子组件
  - 组件间通过 ref 和 expose 通信，而非事件/状态
- **位置**: `HomeView.vue:152-166`
- **优化方案**: 使用 Vue Router 进行页面导航，而非条件渲染

#### 9. 日志级别配置不合理
- **问题**: 生产环境使用 `InfoLevel`，但大量调试信息用 `Info` 级别记录
- **位置**: `utils/logger.go`, 多处 Service/DAO 使用 `utils.Info` 记录调试信息
- **优化方案**: 将调试信息改为 `utils.Debug`，生产信息保留 `utils.Info`

#### 10. PDFImporter 中 PDF 库版本依赖问题
- **问题**: 同时使用 `github.com/gen2brain/go-fitz` 和 `github.com/oliverpool/unipdf`
- **位置**: `pdf_importer.go:20-22`
- **优化方案**: 统一使用单一 PDF 库，减少依赖体积

#### 11. 缺少数据验证
- **问题**: 
  - 后端缺少用户输入验证（如 category 参数）
  - 前端缺少表单验证（如文件上传格式验证）
- **优化方案**: 添加输入验证中间件/装饰器

#### 12. 数据库缺少索引
- **问题**: `error_questions` 和 `favorite_questions` 表的 `user_id + category` 组合查询频繁，但无复合索引
- **优化方案**:
  ```sql
  CREATE INDEX idx_error_questions_user_category ON error_questions(user_id, category);
  CREATE INDEX idx_favorite_questions_user_category ON favorite_questions(user_id, category);
  ```

---

## 五、优化方案优先级排序

### 第一阶段（1-2周内完成）
| 优先级 | 任务 | 预计工作量 |
|--------|------|------------|
| 1 | 修复错题孤立记录问题（方案A：重置题库时清理关联数据） | 2-3小时 |
| 2 | 统一错误处理模式 | 3-4小时 |
| 3 | 创建共享 TypeScript 类型文件 | 1-2小时 |
| 4 | 移除前端 Mock/TODO 代码 | 2-3小时 |

### 第二阶段（2-4周内完成）
| 优先级 | 任务 | 预计工作量 |
|--------|------|------------|
| 5 | 实现真实导入进度（Wails Events） | 4-6小时 |
| 6 | 添加数据库索引 | 1小时 |
| 7 | 配置外键约束 | 2-3小时 |
| 8 | 将考试配置移至数据库 | 6-8小时 |

### 第三阶段（长期优化）
| 优先级 | 任务 | 预计工作量 |
|--------|------|------------|
| 9 | 添加单元测试（目标覆盖率 60%） | 20-30小时 |
| 10 | 使用 Vue Router 重构导航 | 8-12小时 |
| 11 | 统一 PDF 库依赖 | 6-8小时 |
| 12 | 添加输入验证中间件 | 4-6小时 |

---

## 六、代码亮点

1. **架构清晰**: Service-DAO 分层模式，职责明确
2. **SQL注入防护**: 全部使用参数化查询和 map 条件构建
3. **并发处理**: PDF解析使用 goroutine + semaphore，性能优秀
4. **日志系统**: 使用 logrus + lumberjack，支持日志轮转
5. **前端组件化**: Vue 3 Composition API，代码组织良好
6. **用户体验**: 悬浮进度条、数据清空后自动刷新等细节到位

---

## 七、总结

当前代码质量处于**良好**水平，核心业务逻辑稳定，架构设计合理。主要改进方向为：

1. **数据一致性**: 解决孤立记录问题是当务之急
2. **错误处理**: 统一错误处理模式，消除静默失败
3. **类型安全**: 完善 TypeScript 类型定义
4. **测试覆盖**: 建立单元测试体系，保障代码质量

建议按优先级分阶段推进优化，每次迭代完成后进行回归测试。

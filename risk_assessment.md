# 业余无线电模拟考试系统 - 风险评估报告

> 评估日期：2026-04-22
> 评估范围：全项目（后端 Go + 前端 Vue + Wails 桌面应用）
> 评估方式：静态代码审查 + 架构分析 + 依赖审查

---

## 一、风险概览

| 风险等级 | 数量 | 说明 |
|---------|------|------|
| 🔴 **严重 (Critical)** | 2 | 可能导致数据丢失或系统崩溃 |
| 🟠 **高危 (High)** | 5 | 可能导致功能异常或安全风险 |
| 🟡 **中等 (Medium)** | 7 | 影响用户体验或可维护性 |
| 🟢 **低危 (Low)** | 4 | 优化建议，影响较小 |

---

## 二、严重风险 (Critical)

### C-1: 题库重置无事务保护，可能导致数据不一致

| 属性 | 详情 |
|------|------|
| **风险等级** | 🔴 严重 |
| **影响范围** | 题库导入功能 |
| **触发条件** | 导入过程中发生异常（断电/程序崩溃/内存不足） |
| **后果** | 旧题已清空，新题未完全导入，题库为空 |
| **位置** | `backend/dao/question_dao.go:206` |
| **代码** | `db.Exec("DELETE FROM questions")` |

**详细分析**：
```go
// 当前逻辑（无事务）
db.Exec("DELETE FROM questions")        // 步骤1：清空旧数据
db.Exec("INSERT INTO questions ...")    // 步骤2：批量插入新数据
// 如果步骤2失败，步骤1的数据无法恢复
```

**修复方案**：
```go
// 方案A：使用数据库事务
tx := db.Begin()
defer func() {
    if r := recover(); r != nil {
        tx.Rollback()
    }
}()
tx.Exec("DELETE FROM questions")
tx.Exec("INSERT INTO questions ...")
tx.Commit()

// 方案B：先导入临时表，再原子替换
tx := db.Begin()
tx.Exec("CREATE TABLE questions_temp LIKE questions")
tx.Exec("INSERT INTO questions_temp ...")  // 导入到临时表
tx.Exec("DROP TABLE questions")
tx.Exec("RENAME TABLE questions_temp TO questions")
tx.Commit()
```

**建议优先级**：立即修复

---

### C-2: 身份证号明文存储和传输，存在隐私泄露风险

| 属性 | 详情 |
|------|------|
| **风险等级** | 🔴 严重 |
| **影响范围** | 用户登录、用户数据存储 |
| **触发条件** | 数据库文件被窃取或日志泄露 |
| **后果** | 用户身份证号（敏感个人信息）泄露 |
| **位置** | `backend/models/user.go`, `backend/services/user_service.go:43-46` |

**详细分析**：
1. 身份证号以明文存储在 SQLite 数据库文件中
2. 登录时身份证号记录在日志中：`"id_card": idCard`
3. 前端 stores 中定义 `password` 字段（虽然当前未使用）
4. SQLite 数据库文件无加密保护

**修复方案**：
```go
// 方案A：身份证号脱敏存储
import "crypto/sha256"
func hashIDCard(idCard string) string {
    hash := sha256.Sum256([]byte(idCard))
    return fmt.Sprintf("%x", hash)
}

// 方案B：日志中脱敏显示
func maskIDCard(idCard string) string {
    if len(idCard) >= 6 {
        return idCard[:3] + "****" + idCard[len(idCard)-4:]
    }
    return "****"
}
```

**建议优先级**：尽快修复（涉及个人信息保护合规）

---

## 三、高危风险 (High)

### H-1: 登录无认证机制，任何用户可访问所有数据

| 属性 | 详情 |
|------|------|
| **风险等级** | 🟠 高危 |
| **影响范围** | 整个系统 |
| **触发条件** | 用户知道其他用户的身份证号 |
| **后果** | 可访问/修改他人的错题、收藏、考试记录 |
| **位置** | `backend/services/user_service.go:34-127` |

**分析**：
- 登录仅通过身份证号查找，无密码验证
- 身份证号相当于"万能钥匙"，知道即可登录
- 多用户间无数据隔离保护

**修复方案**：
1. 添加密码验证（推荐）
2. 或添加登录频率限制（防暴力猜测）
3. 或限制本机多用户同时使用

**建议优先级**：高（取决于使用场景，单机个人使用可接受）

---

### H-2: 文件导入无大小/类型限制，可能导致 DoS

| 属性 | 详情 |
|------|------|
| **风险等级** | 🟠 高危 |
| **影响范围** | 题库导入功能 |
| **触发条件** | 用户导入超大文件或恶意文件 |
| **后果** | 内存耗尽、程序崩溃 |
| **位置** | `backend/services/import_service.go:238-245` |

**分析**：
```go
// 当前限制
if fileInfo.Size() > 100*1024*1024 {  // 100MB
    // 仅检查大小，但 PDF 解压后可能远超此值
}
```

**修复方案**：
1. 限制并发导入数量（防止同时导入多个大文件）
2. 添加内存使用监控，超限时中止导入
3. 限制单个 PDF 文件页数（如不超过 500 页）

**建议优先级**：高

---

### H-3: 数据库连接无健康检查，故障时无法恢复

| 属性 | 详情 |
|------|------|
| **风险等级** | 🟠 高危 |
| **影响范围** | 所有数据库操作 |
| **触发条件** | 数据库文件被删除/损坏/权限变更 |
| **后果** | 程序持续运行但所有功能失效，用户无法察觉 |
| **位置** | `backend/dao/db_pool.go` |

**修复方案**：
```go
// 添加数据库健康检查
func (p *DBPool) HealthCheck() error {
    sqlDB := p.db.DB
    if sqlDB == nil {
        return fmt.Errorf("数据库连接未初始化")
    }
    return sqlDB.Ping()
}

// 关键操作前检查
func (dao *BaseDAO) safeQuery(...) {
    if err := HealthCheck(); err != nil {
        // 尝试重新连接
        Reconnect()
    }
}
```

**建议优先级**：高

---

### H-4: 考试提交答案无并发控制，可能导致数据覆盖

| 属性 | 详情 |
|------|------|
| **风险等级** | 🟠 高危 |
| **影响范围** | 考试功能 |
| **触发条件** | 用户快速连续点击提交答案 |
| **后果** | 部分答案丢失或重复记录 |
| **位置** | `backend/services/exam_service.go:300+` |

**修复方案**：
```go
// 方案A：使用数据库唯一约束防止重复
// 方案B：添加防重复提交逻辑
type ExamService struct {
    submitting sync.Map  // map[examID]sync.Mutex
}
```

**建议优先级**：中高

---

### H-5: PDF 解析库依赖外部系统库 (libmupdf)

| 属性 | 详情 |
|------|------|
| **风险等级** | 🟠 高危 |
| **影响范围** | PDF 题库导入 |
| **触发条件** | 系统缺少 libmupdf 或版本不兼容 |
| **后果** | 题库导入功能完全不可用 |
| **位置** | `github.com/gen2brain/go-fitz` |

**分析**：
- go-fitz 依赖系统安装的 MuPDF 库
- Windows 用户需要手动安装或捆绑 DLL
- 不同系统/架构需要不同的编译配置

**修复方案**：
1. 在 README 中明确说明系统依赖
2. 提供预编译的 DLL 文件
3. 添加启动时依赖检查，给出友好提示

**建议优先级**：中高

---

## 四、中等风险 (Medium)

### M-1: 错题/收藏数据可能不一致

**风险等级**：🟡 中等
**位置**：`backend/dao/error_question_dao.go`, `backend/dao/favorite_question_dao.go`
**问题**：题目删除后，错题和收藏记录成为孤立数据
**修复**：已添加 LEFT JOIN 和题库重置时清理逻辑（已完成）

---

### M-2: 前端类型定义与后端不完全一致

**风险等级**：🟡 中等
**位置**：`frontend/src/types/index.ts` vs `backend/models/`
**问题**：前端手动定义的类型可能与后端 Go struct 不同步
**修复**：已创建 `@/types/index.ts` 共享类型（已部分修复）

---

### M-3: 日志包含敏感信息

**风险等级**：🟡 中等
**位置**：`backend/services/user_service.go:43-46`
**问题**：身份证号明文记录在日志中
**修复**：日志中添加脱敏处理

---

### M-4: 缺少数据备份机制

**风险等级**：🟡 中等
**位置**：全局
**问题**：用户数据（错题/收藏/考试记录）无自动备份
**修复**：添加定期备份功能或导出功能

---

### M-5: 错误信息可能暴露内部细节

**风险等级**：🟡 中等
**位置**：`backend/services/*.go`
**问题**：部分错误信息直接返回给前端，可能暴露内部结构
**修复**：统一错误响应格式，内部细节仅记录日志

---

### M-6: 单元测试覆盖率为 0

**风险等级**：🟡 中等
**位置**：全局
**问题**：无任何 `*_test.go` 文件，代码变更无法自动验证
**修复**：为核心 DAO/Service 添加单元测试

---

### M-7: 依赖版本锁定不完整

**风险等级**：🟡 中等
**位置**：`go.mod`, `package.json`
**问题**：部分间接依赖未显式锁定版本
**修复**：运行 `go mod tidy` 和 `npm audit`

---

## 五、低危风险 (Low)

### L-1: 前端 console.log 可能泄露调试信息

**风险等级**：🟢 低
**位置**：多个 `.vue` 文件
**问题**：生产版本可能包含调试日志

### L-2: 配置硬编码

**风险等级**：🟢 低
**位置**：`backend/config/exam_config.go`
**问题**：考试配置写死在代码中（已添加 SetExamConfig）

### L-3: PDF 库依赖两个不同库

**风险等级**：🟢 低
**位置**：`go.mod`
**问题**：go-fitz + unipdf 两个库增加体积（评估后建议保持现状）

### L-4: 前端组件耦合度高

**风险等级**：🟢 低
**位置**：`HomeView.vue`
**问题**：使用条件渲染而非 Vue Router 导航

---

## 六、修复方案优先级排序

### 第一阶段：立即修复（1-2 天）

| 编号 | 风险 | 工作量 | 说明 |
|------|------|--------|------|
| C-1 | 题库重置无事务保护 | 2-3 小时 | 使用事务保护数据一致性 |
| M-3 | 日志脱敏 | 1-2 小时 | 身份证号等敏感信息脱敏 |
| M-7 | 依赖版本锁定 | 30 分钟 | 运行 `go mod tidy` + `npm audit fix` |

### 第二阶段：近期修复（1-2 周）

| 编号 | 风险 | 工作量 | 说明 |
|------|------|--------|------|
| C-2 | 身份证号加密存储 | 4-6 小时 | 哈希存储 + 日志脱敏 |
| H-1 | 登录认证机制 | 6-8 小时 | 添加密码验证 |
| H-2 | 文件导入限制 | 2-3 小时 | 并发限制 + 内存监控 |
| H-3 | 数据库健康检查 | 3-4 小时 | 添加连接检查和自动恢复 |
| M-6 | 单元测试 | 20-30 小时 | 核心模块测试覆盖 |

### 第三阶段：长期优化（1-2 月）

| 编号 | 风险 | 工作量 | 说明 |
|------|------|--------|------|
| H-4 | 考试并发控制 | 4-6 小时 | 防重复提交 |
| H-5 | PDF 库依赖管理 | 6-8 小时 | 依赖检查 + 预编译 DLL |
| M-4 | 数据备份机制 | 8-12 小时 | 自动备份 + 导出功能 |
| L-1~L-4 | 低危优化 | 8-12 小时 | 综合优化 |

---

## 七、总体评估

### 当前安全评分：⭐⭐⭐ 3/5

### 主要优势
1. SQL 注入防护完善（全部参数化查询）
2. 错误处理机制基本健全
3. 日志系统完善（logrus + lumberjack）

### 主要短板
1. 缺乏事务保护（数据一致性风险）
2. 个人敏感信息未加密
3. 缺少自动备份机制
4. 无自动化测试

### 建议
1. **立即处理**：C-1（事务保护）和 M-3（日志脱敏）
2. **重点投入**：C-2（身份证号加密）和 H-1（登录认证）
3. **持续改进**：建立单元测试体系，定期安全审查

---

## 八、附录：修复状态追踪

| 编号 | 风险 | 状态 | 修复日期 | 备注 |
|------|------|------|----------|------|
| M-1 | 错题孤立记录 | ✅ 已修复 | 2026-04-21 | LEFT JOIN + 清理逻辑 |
| M-2 | 前端类型定义 | ✅ 已修复 | 2026-04-21 | @/types/index.ts |
| C-1 | 题库重置事务 | ⏳ 待修复 | - | 建议立即处理 |
| C-2 | 身份证号加密 | ⏳ 待修复 | - | 建议近期处理 |
